import 'package:flutter/material.dart';

class Labrary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Labrary'),
      ),
      body: Center(
        child: Text('Labrary'),
      ),
    );
  }
}
